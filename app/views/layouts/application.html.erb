<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Investment Portfolio Rails" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <!-- Navigation Bar -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="container mx-auto px-5">
        <div class="flex justify-between items-center h-16">
          <!-- Logo/Brand -->
          <div class="flex items-center space-x-8">
            <%= link_to root_path, class: "text-xl font-bold text-indigo-600" do %>
              Investment Portfolio
            <% end %>

            <!-- Main Navigation (logged in users only) -->
            <% if user_signed_in? %>
              <div class="flex space-x-6">
                <%= link_to "Portfolios", portfolios_path, class: "text-sm font-medium text-gray-700 hover:text-indigo-600" %>
              </div>
            <% end %>
          </div>

          <!-- Navigation Links -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <span class="text-sm text-gray-600">Welcome, <%= current_user.full_name %>!</span>
              <%= link_to "My Account", edit_user_registration_path, class: "text-sm text-gray-700 hover:text-indigo-600" %>
              <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-sm text-gray-700 hover:text-indigo-600" %>
            <% else %>
              <%= link_to "Sign Up", new_user_registration_path, class: "text-sm text-gray-700 hover:text-indigo-600" %>
              <%= link_to "Log In", new_user_session_path, class: "text-sm bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice.present? %>
      <div class="container mx-auto px-5 mt-4">
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded relative" role="alert">
          <p class="notice"><%= notice %></p>
        </div>
      </div>
    <% end %>
    <% if alert.present? %>
      <div class="container mx-auto px-5 mt-4">
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded relative" role="alert">
          <p class="alert"><%= alert %></p>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="container mx-auto px-5 py-8">
      <%= yield %>
    </main>
  </body>
</html>
